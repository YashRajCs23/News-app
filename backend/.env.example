# Backend environment example
PORT=5000
NODE_ENV=production
MONGODB_URI=mongodb://<user>:<pass>@host:port/dbname
JWT_SECRET=your_jwt_secret_here
FRONTEND_URL=https://your-frontend-url.example
# If you deploy frontend separately and want the API base available to frontend builds
# (optional)
VITE_API_URL=https://your-api-url.example
